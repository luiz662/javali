var undMacas = Number(prompt(" Digite quantas maçãs vocevai comprar "))
var precoUndMaca = 1.30

if (undMacas >= 12) {
     precoUndMaca =1 
        var calculo = undMacas * precoUndMaca 
        alert  ( ` voce comprou ${undMacas} de maçãs e saiu na promoção.
            total =${calculo}`)
    } else {var calculo = undMacas * precoUndMaca
        alert (` voce comprou ${undMacas} de maçãs `)
        total = $ {calculo}
    }


